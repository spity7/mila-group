export interface AboutExperience {
  years: number;
  label: string;
}

export interface AboutContent {
  subHeading: string;
  experience: AboutExperience;
  title: string;
  description: string;
  buttonText: string;
  buttonHref: string;
}
